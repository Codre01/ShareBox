# Design assets

- `nocturne/` — Nocturne design-system stylesheet (offline; no Google Fonts CDN).
- `prototypes/` — Original interactive HTML prototypes (Control Center + Web Client). Clipboard UI in prototypes is **out of V1 scope**.
